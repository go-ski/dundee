% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compare.R
\name{dd_compare_spec}
\alias{dd_compare_spec}
\title{The field spec the comparison helpers and the review app share.}
\usage{
dd_compare_spec()
}
\value{
A data.frame with one row per comparable field: \code{section}, \code{field},
\code{label}, \code{better} (the direction, or \code{""} where there is none) and \code{card}.
}
\description{
Exported because \code{inst/shiny/app.R} calls it, and \code{shiny::runApp()} sources
the app with only \code{library(dundee)} attached: an internal function is
invisible there however cleanly it resolves under \verb{R CMD check}, whose tests
run inside the package namespace.
}
\details{
\code{card = TRUE} marks the fields shown under every thumbnail whether or not the
copies agree: location and capture date are what decisions turn on, so they
are never folded away. They are excluded from the collapsed "identical" line
so nothing appears twice, but still show as a table row when they differ.
}

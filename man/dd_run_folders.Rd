% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run.R
\name{dd_run_folders}
\alias{dd_run_folders}
\title{Report duplicate groups summarised by the directories they span.}
\usage{
dd_run_folders(config = NULL, depth = 1L, effect = FALSE, quiet = FALSE)
}
\arguments{
\item{config}{A work directory, a config path, or a list from \code{\link[=dd_config]{dd_config()}}.}

\item{depth}{Path segments to keep from each \code{rel_path}.}

\item{effect}{Logical; also audit \code{folder_priority} against the current
\code{preference_rules}.}

\item{quiet}{Logical; suppress the printed report. Unlike the other
\verb{dd_run_*()} stages, whose \code{quiet} drops only banners and progress, here
the report \emph{is} the output -- so this silences everything and leaves the
return value to work with. Errors are still raised.}
}
\value{
Invisibly, a list with \code{patterns} and (when asked) \code{effect}.
}
\description{
Prints the \code{\link[=dd_folder_patterns]{dd_folder_patterns()}} table, and with \code{effect = TRUE} the
\code{\link[=dd_folder_effect]{dd_folder_effect()}} audit of the config's \code{folder_priority}. Reads only.
}
\examples{
\dontrun{
dd_run_folders()                       # groups summarised by directory
dd_run_folders(depth = 2)              # look inside single-directory patterns
dd_run_folders(effect = TRUE)          # audit folder_priority before applying

pat <- dd_run_folders(quiet = TRUE)$patterns   # the table, nothing printed
}
}

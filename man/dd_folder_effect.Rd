% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/folders.R
\name{dd_folder_effect}
\alias{dd_folder_effect}
\title{Preview what a folder ranking would decide, and where it would disagree.}
\usage{
dd_folder_effect(
  con,
  cfg,
  priority = cfg$folder_priority,
  depth = 1L,
  quiet = FALSE
)
}
\arguments{
\item{con}{A DBIConnection.}

\item{cfg}{A config list; \code{preference_rules} supplies the comparison and the
tie-break.}

\item{priority}{Character vector of folders relative to \code{library_root},
most-preferred first. Defaults to \code{cfg$folder_priority}.}

\item{depth}{Path segments to keep when building the pattern table.}

\item{quiet}{Logical; suppress the per-group progress bar.}
}
\value{
Invisibly, a list with \code{by_folder}, \code{tied}, \code{n_groups}, a \code{differs}
data.frame (\code{group_id}, \code{folder_winner}, \code{rules_winner}) and \code{patterns},
the \code{\link[=dd_folder_patterns]{dd_folder_patterns()}} table plus \code{winner} and \code{n_differs}.
}
\description{
Reports how much of the library \code{priority} settles on its own, how much it
leaves to the quality rules, and -- the point of the function -- every group
where it would pick a different copy than \code{cfg$preference_rules} picks today.
A folder ranking is usually right where the quality rules are merely
arbitrary, so the disagreements are what deserve a look before
\code{\link[=dd_apply_bulk_decisions]{dd_apply_bulk_decisions()}} writes them.
}
\details{
Nothing is written. To act on the result, put \code{folder_priority} first in
\code{preference_rules}, set \code{folder_priority} to \code{priority}, and re-run
\code{dd_apply_bulk_decisions(con, cfg, overwrite = TRUE)}.
}
\examples{
\dontrun{
cfg <- dd_config("~/dundee/family-photos")
con <- dd_db_connect(cfg)
on.exit(DBI::dbDisconnect(con))

# Try a ranking without committing it to config.yml.
eff <- dd_folder_effect(con, cfg, c("Named", "OldAppLibrary"))
eff$by_folder                        # groups the tree settles on its own
eff$differs                          # every group this would change

# Happy with it? Put folder_priority first in preference_rules, set
# folder_priority to the same ranking, then:
dd_apply_bulk_decisions(con, cfg, overwrite = TRUE)
}
}
\seealso{
\code{\link[=dd_run_folders]{dd_run_folders()}}, which does this from a work directory and prints
the summary.
}

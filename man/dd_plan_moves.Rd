% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/move.R
\name{dd_plan_moves}
\alias{dd_plan_moves}
\title{Plan moves for all decided photos and emit a manifest + shell script.}
\usage{
dd_plan_moves(
  con,
  cfg,
  manifest_path = file.path(cfg$work_dir, "moves.tsv"),
  script_path = file.path(cfg$work_dir, "moves.sh"),
  quiet = FALSE
)
}
\arguments{
\item{con}{A DBIConnection.}

\item{cfg}{A config list.}

\item{manifest_path}{Output TSV path (default under \code{work_dir}).}

\item{script_path}{Output shell script path (default under \code{work_dir}).}

\item{quiet}{Logical; suppress the per-row progress bar.}
}
\value{
The manifest data.frame, invisibly.
}
\description{
Writes nothing destructive and touches nothing in the library: produces a TSV
manifest, a self-contained shell script for you to review and run, and
records planned rows in the \code{moves} table. Re-runnable.
}
\details{
The script takes \code{--dry-run}, refuses to start unless the library is mounted
and writable, and appends every completed move to \code{moves.done.tsv} beside
itself. \code{\link[=dd_run_move]{dd_run_move()}} reads that afterwards to mark the batch done.
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/details.R
\name{dd_review_progress}
\alias{dd_review_progress}
\title{Review progress, counted in groups.}
\usage{
dd_review_progress(con)
}
\arguments{
\item{con}{A DBIConnection.}
}
\value{
A list with \code{decided} and \code{groups}, both integers.
}
\description{
Counts \strong{groups} that carry at least one decision, not decided photos. A
group is reviewed once its preferred copy has been chosen, however many
copies it holds, so groups are the unit the reviewer is working through.
This is deliberately not \code{\link[=dd_status]{dd_status()}}'s \code{decided}, which counts photo rows.
}
\examples{
\dontrun{
cfg <- dd_config("~/dundee/family-photos")
con <- dd_db_connect(cfg)
on.exit(DBI::dbDisconnect(con))

dd_review_progress(con)
}
}

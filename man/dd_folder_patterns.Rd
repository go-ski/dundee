% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/folders.R
\name{dd_folder_patterns}
\alias{dd_folder_patterns}
\title{Summarise duplicate groups by the directories they span.}
\usage{
dd_folder_patterns(con, depth = 1L)
}
\arguments{
\item{con}{A DBIConnection.}

\item{depth}{Path segments to keep from each \code{rel_path}; 1 is the top-level
directory.}
}
\value{
A data.frame with \code{pattern}, \code{spans}, \code{n_groups}, \code{n_photos},
\code{n_exact} and \code{n_near}, ordered by \code{n_groups} descending. Zero rows when
nothing is grouped.
}
\description{
Collapses every group to the sorted set of distinct directories its members
occupy, then counts groups per distinct set. A library with a few dozen
top-level directories yields a few dozen rows however many groups it has.
}
\details{
\code{spans == 1} marks a pattern whose copies all live in one directory, where
\code{folder_priority} has nothing to say and the quality rules or the review app
must decide. Raise \code{depth} to look inside those.
}
\examples{
\dontrun{
cfg <- dd_config("~/dundee/family-photos")
con <- dd_db_connect(cfg)
on.exit(DBI::dbDisconnect(con))

dd_folder_patterns(con)              # top-level directories
dd_folder_patterns(con, depth = 2)   # inside the single-directory patterns
}
}
\seealso{
\code{\link[=dd_run_folders]{dd_run_folders()}}, which does this from a work directory and prints
the table.
}

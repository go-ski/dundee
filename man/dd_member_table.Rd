% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compare.R
\name{dd_member_table}
\alias{dd_member_table}
\title{Merge a group's stored rows with its freshly read details for comparison.}
\usage{
dd_member_table(photos, details = NULL)
}
\arguments{
\item{photos}{A data.frame of stored rows (\code{photo_id}, \code{width}, \code{height},
\code{size}, \code{format}, \code{meta_count}, \code{rel_path}, \code{mtime}).}

\item{details}{A data.frame from \code{\link[=dd_group_details]{dd_group_details()}}, or \code{NULL}.}
}
\value{
A data.frame with one row per copy, ready for \code{\link[=dd_compare_fields]{dd_compare_fields()}}.
}
\description{
Derives the fields that are only interesting as a comparison -- pixel count,
bytes per pixel, the containing folder -- and lets the freshly read metadata
win over the two columns the store happens to duplicate.
}

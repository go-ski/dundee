% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fingerprint.R
\name{dd_alpha_photos}
\alias{dd_alpha_photos}
\title{Photos whose stored fingerprint predates the alpha fix.}
\usage{
dd_alpha_photos(con, quiet = FALSE)
}
\arguments{
\item{con}{A DBIConnection.}

\item{quiet}{Logical; suppress the progress bar.}
}
\value{
Character vector of paths needing a fresh fingerprint.
}
\description{
Finds the photos an algorithm bump actually affects, so a repair re-reads
those and not the whole library. Two stages, cheapest first: JPEG carries no
alpha channel at all, so \code{jpegload} rows are excluded without touching the
filesystem; every other row is then settled by reading its \emph{header} only, at
no cost in transferred pixels.
}
\details{
Photos whose file has gone are skipped -- a missing file is inventory's
business, not this function's.
}
\examples{
\dontrun{
cfg <- dd_config("~/dundee/family-photos")
con <- dd_db_connect(cfg)
on.exit(DBI::dbDisconnect(con))

length(dd_alpha_photos(con))
}
}
